apiVersion: v1
kind: Service
metadata:
  name: meta-model-frontend-service
spec:
  selector:
    app: meta-model-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
