apiVersion: v1
kind: Service
metadata:
  name: meta-model-mongodb-service
  namespace: meta-model
spec:
  selector:
    app: meta-model-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
