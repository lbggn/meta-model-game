apiVersion: v1
kind: Service
metadata:
  name: meta-model-backend-service
spec:
  selector:
    app: meta-model-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
